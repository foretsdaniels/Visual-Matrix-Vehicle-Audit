[project]
name = "parking-audit-builder"
version = "1.0.0"
description = "Visual Matrix Vehicle Audit - Parking Audit Builder"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
